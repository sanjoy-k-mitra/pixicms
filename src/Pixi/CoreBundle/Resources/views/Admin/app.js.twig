angular.module("pixiAdminApp", ["ui.router", "pixi.admin"])
.config(['$stateProvider', '$urlRouterProvider', function ($stateProvider, $urlRouterProvider) {

$urlRouterProvider.otherwise('dashboard')

$stateProvider
{% for route in routes %}
    .state('{{ route.name }}', {
        templateUrl:'{{route.path}}',
        url: '{{ route.sref }}'
    })
{% endfor %}
}]);